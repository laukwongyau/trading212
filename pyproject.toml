[project]
name = "trading212-portfolio-tracker"
version = "0.1.0"
description = "A Streamlit dashboard for Trading212 holdings, ROI, and recent activity."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.60.0",
    "requests>=2.32.0",
    "pandas>=2.2.0",
    "plotly>=5.24.0",
]

[tool.uv]
package = false
